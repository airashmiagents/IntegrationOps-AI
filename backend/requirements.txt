# IntegrationOps-AI backend — FastAPI MVP (Render / local).
# Entry: main:app  →  uvicorn main:app --host 0.0.0.0 --port $PORT
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
openai>=1.0.0
python-dotenv>=1.0.0
pydantic-settings>=2.1.0
httpx>=0.26.0
apscheduler>=3.10.0
